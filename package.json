{
  "name": "blogging-scrapper-blog",
  "version": "1.0.0",
  "description": "an automated way to scrape sites and upload to medium",
  "main": "./index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "start:build": "npm run install-client && npm run build && npm install",
    "start": "node index"
  },
  "author": "Harrison Osagiede",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.1.2",
    "bull": "^4.10.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "ioredis": "^5.2.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
